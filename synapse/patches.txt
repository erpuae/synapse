[pre_model_sync]
# Patches executed before DocTypes are migrated.

[post_model_sync]
# Patches executed after DocTypes are migrated.
# Roles are created by the after_install hook, not from here — see synapse/install.py.
