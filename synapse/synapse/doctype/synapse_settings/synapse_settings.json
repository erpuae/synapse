{
 "actions": [],
 "creation": "2026-08-19 00:00:00.000000",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "enabled",
  "intro_html",
  "section_model",
  "model_provider",
  "model_html",
  "section_tools",
  "enable_read_tools",
  "enable_write_tools",
  "column_break_tools",
  "enable_sql_tool",
  "enable_custom_tools",
  "max_rows",
  "date_format",
  "section_access",
  "access_html",
  "denylist_html",
  "denied_doctypes",
  "section_log",
  "log_payloads",
  "log_retention_days"
 ],
 "fields": [
  {"fieldname": "enabled", "fieldtype": "Check", "label": "Enable Synapse Endpoint", "default": "0", "description": "Master switch. Off means every tool refuses, whatever else is configured here."},
  {"fieldname": "intro_html", "fieldtype": "HTML", "options": "<p class=\"text-muted small\">Tools are exposed at <code>/api/method/synapse.mcp.handle_mcp</code>. What a caller can reach is decided by the <b>Synapse Profile</b> records whose roles they hold, unioned together. Frappe permissions always apply on top, they are never bypassed. Raw SQL is the exception, and it ignores permissions entirely.</p>"},

  {"fieldname": "section_model", "fieldtype": "Section Break", "label": "Model"},
  {"fieldname": "model_provider", "fieldtype": "Select", "label": "Model Provider", "options": "Claude\nOpenAI\nGemini", "default": "Claude", "description": "The model family this endpoint is presented for. Only Claude is wired up today; the rest are placeholders."},
  {"fieldname": "model_html", "fieldtype": "HTML", "options": "<p class=\"text-muted small\">This setting does not change how tools run. It records which model family the site is presenting Synapse for. Support beyond Claude is not implemented yet.</p>"},

  {"fieldname": "section_tools", "fieldtype": "Section Break", "label": "Tools"},
  {"fieldname": "enable_read_tools", "fieldtype": "Check", "label": "Enable Read Tools", "default": "1", "description": "get_doc, get_value, get_list, get_count, describe_doctype, list_available_doctypes"},
  {"fieldname": "enable_write_tools", "fieldtype": "Check", "label": "Enable Write Tools", "default": "0", "description": "create_doc, update_doc, set_value, submit_doc, cancel_doc, delete_doc, the child-table tools, replace_in_field and run_operation"},
  {"fieldname": "column_break_tools", "fieldtype": "Column Break"},
  {"fieldname": "enable_sql_tool", "fieldtype": "Check", "label": "Enable Read-Only SQL Tool", "default": "0", "description": "Also needs a Synapse Profile with 'Allow SQL' ticked. Raw SQL ignores all DocType permissions."},
  {"fieldname": "enable_custom_tools", "fieldtype": "Check", "label": "Enable Custom Tools", "default": "0", "description": "Turn on tools contributed by other installed apps. Each still has to be named in a Synapse Profile. Off means no custom tool runs, whatever a profile lists."},
  {"fieldname": "max_rows", "fieldtype": "Int", "label": "Row Limit", "default": "100", "description": "Rows any single call may return. Capped at 500 by the app."},
  {"fieldname": "date_format", "fieldtype": "Select", "label": "Date Format", "options": "ISO (YYYY-MM-DD)\nDD-MM-YYYY", "default": "ISO (YYYY-MM-DD)", "description": "How dates are returned to the client. Writes always accept either form, so changing this cannot break a round trip."},

  {"fieldname": "section_access", "fieldtype": "Section Break", "label": "Access"},
  {"fieldname": "access_html", "fieldtype": "HTML", "options": "<p class=\"text-muted small\">Access is granted by <b>Synapse Profile</b> records, not here. A user's reach is the union of every enabled profile whose roles they hold. With no profile, nothing is reachable. The list below is a site-wide <b>backstop</b>: it blocks actions no agent may take whatever a profile grants.</p>"},
  {"fieldname": "denylist_html", "fieldtype": "HTML", "options": "<p class=\"text-muted small\">Token and credential DocTypes are always blocked, and schema, code and permission DocTypes (DocType, Custom Field, Server Script, Custom DocPerm, Role, User ...) are always read only, whether or not they appear below. Add a row only to block something a profile would otherwise allow.</p>"},
  {"fieldname": "denied_doctypes", "fieldtype": "Table", "label": "Blocked DocTypes (backstop)", "options": "Synapse Denied DocType"},

  {"fieldname": "section_log", "fieldtype": "Section Break", "label": "Audit Log"},
  {"fieldname": "log_payloads", "fieldtype": "Check", "label": "Log Field Values", "default": "1", "description": "Store submitted values and before/after changes on each Synapse Log row. Password-like fields are always masked. Untick if the data itself must not be duplicated into the log."},
  {"fieldname": "log_retention_days", "fieldtype": "Int", "label": "Retention (Days)", "default": "90", "description": "A daily job drops Synapse Log rows older than this."}
 ],
 "index_web_pages_for_search": 0,
 "issingle": 1,
 "links": [],
 "modified": "2026-08-25 00:00:00.000000",
 "modified_by": "Administrator",
 "module": "Synapse",
 "name": "Synapse Settings",
 "owner": "Administrator",
 "permissions": [
  {"role": "System Manager", "read": 1, "write": 1, "create": 1, "print": 1, "email": 1, "share": 1}
 ],
 "sort_field": "creation",
 "sort_order": "DESC",
 "states": [],
 "track_changes": 1
}
